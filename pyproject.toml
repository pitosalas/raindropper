[project]
name = "raindropper"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.5",
]

[project.scripts]
raindropper = "raindropper.main:main"

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
