# Tasks for F03 — List Single-Use Tags

### T01 — Implement single-use tag filter
**Status**: not started
**Description**: Create `raindropper/actions.py` with a `list_single_use_tags(client)` function. Call `client.fetch_tags()`, filter to tags where `count == 1`, and print them as a plain numbered list (e.g. `1. tagname`).

### T02 — Wire action into menu
**Status**: not started
**Description**: In `raindropper/main.py`, add "List single-use tags" as menu option 1 and call `list_single_use_tags(client)` when selected. Pass the `RaindropClient` instance to the action.

### T03 — Write tests for single-use tag filter
**Status**: not started
**Description**: In `tests/test_actions.py`, test `list_single_use_tags` with a mock client that returns a mix of tags with varying counts. Assert that only tags with `count == 1` appear in the printed output, using `unittest.mock.patch("builtins.print")`.
