# Tasks for F11 — Split Multi-Word Tags on Bookmarks

### T01 — Add fetch_bookmarks_by_tag to client
**Status**: done
**Description**: Add `fetch_bookmarks_by_tag(tag)` to `RaindropClient`. Calls `GET /raindrops/0` with search param `[{"key":"tag","val":tag}]`. Returns list of bookmark objects.

### T02 — Add update_bookmark_tags to client
**Status**: done
**Description**: Add `update_bookmark_tags(bookmark_id, tags)` to `RaindropClient`. Calls `PUT /raindrop/{id}` with `{"tags": tags}`.

### T03 — Implement split_multiword_tags action
**Status**: done
**Description**: Add `split_multiword_tags(client, selection_set)` to `raindropper/actions.py`. Guard against empty set and non-tag kind. Prompt for confirmation. For each multi-word tag, fetch bookmarks using that tag, remove the multi-word tag, add each word only if not already present, update bookmark. Print count of updated bookmarks.

### T04 — Add to menu
**Status**: done
**Description**: Add `("Split multi-word tags on bookmarks", "split_multiword_tags")` to `MENU_OPTIONS` in `raindropper/main.py`.

### T05 — Write tests
**Status**: done
**Description**: In `tests/test_actions.py`, test confirmed split, skipping already-present words, aborted split, and empty set guard.
