# Tasks for F03 — Selection Set

### T01 — Create SelectionSet class
**Status**: done
**Description**: Create `raindropper/selection_set.py` with a `SelectionSet` class. It should hold a list of items, expose `add_all(items)` to extend the list, `items()` to return the list, and `clear()` to reset it.

### T02 — Write tests for SelectionSet
**Status**: done
**Description**: In `tests/test_selection_set.py`, test `add_all`, `items`, and `clear`. Use plain lists as items (no mocking needed).

### T03 — Thread SelectionSet through run_menu
**Status**: done
**Description**: In `raindropper/main.py`, instantiate a `SelectionSet` in `main()` and pass it alongside `client` to each action handler. Update `run_menu` signature to accept `selection_set` and pass it as a second argument in the dispatch call: `getattr(actions, handler_name)(client, selection_set)`.
