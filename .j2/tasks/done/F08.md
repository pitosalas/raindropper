# Tasks for F08 — Select Bookmarks with Single-Use Tags and Other Tags

### T01 — Add fetch_bookmarks to client
**Status**: done
**Description**: Add `fetch_bookmarks()` to `RaindropClient` in `raindropper/client.py`. Fetch all bookmarks from `/raindrops/0` with pagination (perpage=50, increment page until fewer than perpage items returned). Return list of bookmark objects.

### T02 — Implement action
**Status**: done
**Description**: Add `select_bookmarks_with_mixed_tags(client, selection_set)` to `raindropper/actions.py`. Fetch tags to build a set of single-use tag names. Fetch all bookmarks. Filter to bookmarks that have at least one single-use tag AND at least one non-single-use tag. Call `selection_set.add_all(matches, kind="bookmarks")`. Print count added.

### T03 — Add to menu
**Status**: done
**Description**: Add `("Select bookmarks with mixed tags", "select_bookmarks_with_mixed_tags")` to `MENU_OPTIONS` in `raindropper/main.py`.

### T04 — Write tests
**Status**: done
**Description**: In `tests/test_actions.py`, test `select_bookmarks_with_mixed_tags` with a mock client. Use tags with mixed counts and bookmarks with various tag combinations. Assert only bookmarks with at least one single-use AND one non-single-use tag are selected.
