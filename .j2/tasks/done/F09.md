# Tasks for F09 — Delete Tags in Selection Set

### T01 — Add delete_tags to client
**Status**: done
**Description**: Add `delete_tags(tag_ids)` to `RaindropClient` in `raindropper/client.py`. Call `DELETE /tags` with `{"tags": tag_ids}` and raise on error.

### T02 — Implement delete_selection_set_tags action
**Status**: done
**Description**: Add `delete_selection_set_tags(client, selection_set)` to `raindropper/actions.py`. Abort if empty or kind != "tags". Prompt "About to delete N tag(s). Proceed? [y/N]" and abort if not confirmed. Call `client.delete_tags`, clear the set, print count deleted.

### T03 — Add to menu
**Status**: done
**Description**: Add `("Delete tags in selection set", "delete_selection_set_tags")` to `MENU_OPTIONS` in `raindropper/main.py`.

### T04 — Write tests
**Status**: done
**Description**: In `tests/test_actions.py`, test confirmed delete, aborted delete, empty set, and wrong kind. Assert API called only on confirmation and set cleared after deletion.
