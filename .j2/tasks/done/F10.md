# Tasks for F10 — Select Multi-Word Tags into Selection Set

### T01 — Implement select_multiword_tags action
**Status**: done
**Description**: Add `select_multiword_tags(client, selection_set)` to `raindropper/actions.py`. Fetch all tags, filter to those with a space in `_id` where no individual word is gibberish (per `_is_gibberish`). Add to selection set with kind="tags". Print count.

### T02 — Add to menu
**Status**: done
**Description**: Add `("Select multi-word tags", "select_multiword_tags")` to `MENU_OPTIONS` in `raindropper/main.py`.

### T03 — Write tests
**Status**: done
**Description**: In `tests/test_actions.py`, test `select_multiword_tags` with multi-word real tags, single-word tags, and multi-word tags containing gibberish words. Assert only clean multi-word tags are selected.
