# Tasks for F13 — Select Single-Use Tags That Are Not the Only Tag on a Bookmark

### T01 — Implement select_nonsolitary_single_use_tags action
**Status**: done
**Description**: Add `select_nonsolitary_single_use_tags(client, selection_set)` to `raindropper/actions.py`. Fetch all tags to build a set of single-use tag names (count == 1). Fetch all bookmarks. Build a lookup: tag name → list of bookmarks that carry it. For each single-use tag, find its bookmark(s) and include the tag only if at least one of those bookmarks has more than one tag. Add matching tag objects to the selection set (kind="tags"). Print how many were added.

### T02 — Add to menu
**Status**: done
**Description**: Add `("Select single-use tags with other tags", "select_nonsolitary_single_use_tags")` to `MENU_OPTIONS` in `raindropper/main.py`.

### T03 — Write tests
**Status**: done
**Description**: In `tests/test_actions.py`, test with a mock client returning: (a) a single-use tag on a bookmark that also has other tags → selected; (b) a single-use tag on a bookmark with no other tags → not selected; (c) no single-use tags → empty result. Assert selection set contents and printed count.
