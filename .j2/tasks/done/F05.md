# Tasks for F05 — Print Selection Set

### T01 — Implement print_selection_set action
**Status**: done
**Description**: In `raindropper/actions.py`, add `print_selection_set(client, selection_set)`. If the set is empty print "Selection set is empty." Otherwise print each item as a numbered list using its `tag` field (e.g. `1. tagname`).

### T02 — Add to menu
**Status**: done
**Description**: In `raindropper/main.py`, append `("Print selection set", "print_selection_set")` to `MENU_OPTIONS`.

### T03 — Write tests for print_selection_set
**Status**: done
**Description**: In `tests/test_actions.py`, test `print_selection_set` with an empty selection set (assert "empty" message) and with a populated set (assert items appear in output). Use `unittest.mock.patch("builtins.print")`.
