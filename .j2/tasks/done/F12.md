# Tasks for F12 — Progress Dots for Multi-Step Operations

### T01 — Add progress dot to fetch_bookmarks pagination loop
**Status**: done
**Description**: In `raindropper/client.py`, print `.` (no newline, flush=True) after each page is fetched inside the `fetch_bookmarks` while loop. Print a newline after the loop ends. This gives feedback during large bookmark fetches used by `select_bookmarks_with_mixed_tags`.

### T02 — Add progress dot to split_multiword_tags bookmark loop
**Status**: done
**Description**: In `raindropper/actions.py`, print `.` (no newline, flush=True) after each `update_bookmark_tags` call inside `split_multiword_tags`. Print a newline after all bookmarks are processed.

### T03 — Write tests for progress output
**Status**: done
**Description**: In the relevant test functions, assert that `.` is printed during iteration and a newline follows. Use `patch("builtins.print")` and check call args.
