# Tasks for F07 — Remove Stop Words from Selection Set

### T01 — Define stop words list
**Status**: done
**Description**: In `raindropper/actions.py`, define `STOP_WORDS` as a set of common English words (a, an, the, and, or, but, of, in, on, at, to, for, with, by, from, is, it, as, be, this).

### T02 — Implement remove_stop_words action
**Status**: done
**Description**: Add `remove_stop_words(client, selection_set)` to `raindropper/actions.py`. Filter out items whose `_id` is in `STOP_WORDS`, clear the set, re-add kept items preserving kind. Print count removed.

### T03 — Add to menu
**Status**: done
**Description**: Add `("Remove stop words", "remove_stop_words")` to `MENU_OPTIONS` in `raindropper/main.py`.

### T04 — Write tests
**Status**: done
**Description**: In `tests/test_actions.py`, test `remove_stop_words` with matching and non-matching tags. Assert correct items removed and count message printed.
