# Tasks for F04 — Select Single-Use Tags into Selection Set

### T01 — Update action to populate selection set
**Status**: done
**Description**: In `raindropper/actions.py`, replace `list_single_use_tags(client)` with `select_single_use_tags(client, selection_set)`. Filter tags where `count == 1`, call `selection_set.add_all(single_use_tags)`, and print how many tags were added (e.g. "Added 5 tag(s) to selection set.").

### T02 — Update menu registration
**Status**: done
**Description**: In `raindropper/main.py`, update `MENU_OPTIONS` to use `("Select single-use tags", "select_single_use_tags")` so the new action name is dispatched correctly.

### T03 — Write tests for select_single_use_tags
**Status**: done
**Description**: In `tests/test_actions.py`, test `select_single_use_tags` with a mock client returning tags with mixed counts and a real `SelectionSet`. Assert the selection set contains only tags with `count == 1` and that the correct count message is printed.
